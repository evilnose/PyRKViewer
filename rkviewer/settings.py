# schema validates user input of settings. also has annotation
# manually validate that the schema has defaults, annoations, etc.
# plugins also need to provide function that pre-processes the raw json